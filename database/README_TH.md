# ฐานข้อมูล RGA Dashboard - คู่มือภาษาไทย

## 📁 ไฟล์ในโฟลเดอร์นี้

### ไฟล์ SQL

1. **`setup_rga_dashboard.sql`** ⭐ (ใช้ไฟล์นี้)
   - สคริปต์ SQL สำหรับตั้งค่าฐานข้อมูลทั้งหมด
   - รวม tables, indexes, triggers, functions, และ seed data
   - **แนะนำให้ใช้ไฟล์นี้สำหรับ pgAdmin4**

2. **`schema.sql`**
   - Schema เดิม (อาจไม่ครบทุก table)
   - ใช้เป็น reference

3. **`verify_database.sql`**
   - สคริปต์สำหรับตรวจสอบว่าฐานข้อมูลถูกตั้งค่าถูกต้องหรือไม่
   - รันหลังจาก setup เสร็จ

### ไฟล์เอกสาร

1. **`PGADMIN4_SETUP_GUIDE.md`** ⭐
   - คู่มือการตั้งค่าใน pgAdmin4 แบบละเอียด
   - **อ่านคู่มือนี้ก่อนเริ่มตั้งค่า**

2. **`README.md`**
   - คู่มือภาษาอังกฤษ

---

## 🚀 Quick Start

### ขั้นตอนที่ 1: สร้าง Database

1. เปิด pgAdmin4
2. คลิกขวาที่ "Databases" → Create → Database
3. ตั้งชื่อ: `rga_dashboard`
4. คลิก Save

### ขั้นตอนที่ 2: รัน SQL Script

1. คลิกขวาที่ database `rga_dashboard` → Query Tool
2. เปิดไฟล์ `setup_rga_dashboard.sql`
3. คัดลอกเนื้อหาทั้งหมด (Ctrl+A, Ctrl+C)
4. วางใน Query Tool (Ctrl+V)
5. กด F5 หรือคลิก Execute (▶️)

### ขั้นตอนที่ 3: ตรวจสอบ

รันคำสั่งนี้ใน Query Tool:

```sql
SELECT COUNT(*) as total_tables
FROM information_schema.tables 
WHERE table_schema = 'public';
-- ควรได้ 17
```

---

## 📊 Database Structure

### Tables (17 tables)

1. **tenants** - องค์กร/ลูกค้า (Multi-tenant)
2. **users** - ผู้ใช้งาน
3. **roles** - บทบาทและสิทธิ์
4. **integrations** - การเชื่อมต่อกับแพลตฟอร์มภายนอก
5. **campaigns** - แคมเปญการตลาด
6. **metrics** - ข้อมูลเมตริกแบบ time-series
7. **alerts** - การตั้งค่าแจ้งเตือน
8. **alert_history** - ประวัติการแจ้งเตือน
9. **reports** - รายงาน
10. **ai_insights** - ข้อมูลเชิงลึกจาก AI
11. **ai_queries** - คำถามภาษาไทย
12. **audit_logs** - บันทึกการตรวจสอบ
13. **sessions** - เซสชันผู้ใช้
14. **sync_histories** - ประวัติการซิงค์ข้อมูล
15. **webhook_events** - เหตุการณ์ webhook
16. **activity_logs** - บันทึกกิจกรรมผู้ใช้
17. **oauth_states** - สถานะ OAuth

---

## 🔐 การสร้าง Admin User แรก

สำหรับการใช้งานจริง **ไม่มี seed data** ต้องสร้างเอง:

### วิธีที่ 1: ใช้ API (แนะนำ)
```bash
# 1. สร้าง Tenant
POST /api/v1/tenants
{
  "name": "Your Company",
  "slug": "your-company",
  "subscriptionPlan": "enterprise"
}

# 2. สร้าง Super Admin
POST /api/v1/users
{
  "tenantId": "<tenant_id>",
  "email": "admin@yourcompany.com",
  "password": "YourSecurePassword",
  "role": "super_admin"
}
```

### วิธีที่ 2: ใช้ SQL Script
- ดูไฟล์: `database/create_admin_user.sql`
- แก้ไขข้อมูลตามต้องการ
- รันใน Query Tool

📖 **อ่านคู่มือละเอียด**: `database/PRODUCTION_SETUP.md`

---

## 🔗 Connection String

```env
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/rga_dashboard
```

---

## ✅ Checklist

- [ ] สร้าง database `rga_dashboard` ใน pgAdmin4
- [ ] รัน `setup_rga_dashboard.sql`
- [ ] ตรวจสอบด้วย `verify_database.sql`
- [ ] ตั้งค่า `.env` ใน backend
- [ ] รัน Prisma migrations
- [ ] รัน seed data
- [ ] ทดสอบการเชื่อมต่อ

---

## 📚 เอกสารเพิ่มเติม

- **คู่มือละเอียด**: อ่าน `PGADMIN4_SETUP_GUIDE.md`
- **Prisma Schema**: ดูที่ `backend/prisma/schema.prisma`
- **Migrations**: ดูที่ `backend/prisma/migrations/`

---

## 🆘 ปัญหาที่พบบ่อย

### Permission Denied
```sql
GRANT ALL PRIVILEGES ON DATABASE rga_dashboard TO your_user;
```

### UUID Extension Error
```sql
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
```

### Table Already Exists
- ใช้ `CREATE TABLE IF NOT EXISTS` (มีในสคริปต์แล้ว)
- หรือลบ database และสร้างใหม่

---

## 📞 Support

หากมีปัญหา:
1. ตรวจสอบ error messages ใน Query Tool
2. ดู logs ใน pgAdmin4
3. ตรวจสอบ Prisma schema
4. อ่าน Troubleshooting section ใน `PGADMIN4_SETUP_GUIDE.md`

---

**อัปเดตล่าสุด**: 2025-11-13  
**เวอร์ชัน**: 1.0.0

